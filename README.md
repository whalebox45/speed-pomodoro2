https://roadmap.sh/projects/pomodoro-timer

# Speed Pomodoro

AI-generated content Disclaimer: This project used AI to generate some of the content, such as code generating (mainly the aria attributes and accessibility features), debugging, as well as the icon.

## Run

```sh
npm install
npm run dev
```

Build for production:

```sh
npm run build
npm run preview  # preview the build locally
```

